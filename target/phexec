all: nc

include Makefile.common

SOURCES = \
	$(BINDINGS_SOURCES)\
	$(PINTLIB_SOURCES) \
	$(PHLIB_SOURCES) \
	ui.ml \
	phexec.ml

LIBS := $(PHLIB_LIBS) unix

RESULT = ph-exec

include OCamlMakefile
# vi:set syntax=make:

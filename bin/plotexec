#!/usr/bin/env python

import sys

plots = sys.argv[1:]

print("""
set border 0
set style data steps
set size 1.0,0.3
set ytics 1
set multiplot layout %d,1
""" % len(plots))

for d in sys.argv[1:] :
	t = d.split('.')[0]
	print("plot \"%s\" title \"%s\"" % (d, t))


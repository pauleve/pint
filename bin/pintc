#!/usr/bin/env bash
ocamlopt \
	-I "$PINT_PATH" \
	-I "$PINT_PATH/bindings" \
	-I "$PINT_PATH/pintlib" \
	-I "$PINT_PATH/phlib" \
	-cclib -lm -cclib -lRmath -cclib -lcamlidl\
	nums.cmxa str.cmxa pint.cmxa \
	$*

#	"$PINT_PATH/bindings/r_stubs.o" "$PINT_PATH/bindings/r_impl.o" \

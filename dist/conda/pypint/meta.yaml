{% set name = "pypint" %}
{% set version = "1.3.94" %}
{% set file_ext = "tar.gz" %}
{% set hash_type = "sha256" %}
{% set hash_value = "a64491b8d1bfbcb849cd04bef161107ea6c3d405d4bf15e2fc932d75ef14771f" %}

package:
  name: '{{ name|lower }}'
  version: '{{ version }}'

source:
  fn: '{{ name }}-{{ version }}.{{ file_ext }}'
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.{{ file_ext }}
  '{{ hash_type }}': '{{ hash_value }}'

build:
  number: 2
  entry_points:
    - pint-import=pypint.console:import_model
  script: python setup.py install --single-version-externally-managed --record=record.txt
  noarch: python

requirements:
  build:
    - python
    - setuptools
  run:
    - python >=3.5
    - colomoto_jupyter >=0.4,<0.5
    - ginsim >=2.9.6
    - networkx >=2.0
    - pint >=2017.12.19

test:
  imports:
    - pypint
    - pypint.converters
  commands:
    - pint-import --help

about:
  home: http://loicpauleve.name/pint
  license: CeCILL
  summary: Python interface to Pint
  description: Python interface to Pint

